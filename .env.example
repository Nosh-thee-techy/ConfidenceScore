GEMINI_API_KEY=your_gemini_api_key_here
# Optional — try gemini-2.5-flash if another model hits quota:0 on free tier
# GEMINI_MODEL=gemini-2.5-flash
# Optional — default 8192; raise if long audits truncate (max 16384)
# GEMINI_MAX_OUTPUT_TOKENS=8192
APP_URL="MY_APP_URL"
